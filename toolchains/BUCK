load(":demo_local.bzl", "hxy_system_toolchains")

# Prelude demo toolchains, copied locally so the Rust toolchain can carry
# a -Copt-level flag (see demo_local.bzl) that cc-rs build scripts require.
hxy_system_toolchains()
