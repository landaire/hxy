# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.
#
# VENDOR NOTE: verbatim copy of the egui_ltreeview 0.8.0 crates.io
# tarball with only the egui/eframe/egui_extras requirements bumped
# from 0.35 to 0.36 (upstream has no 0.36 release yet). Wired in via
# [patch.crates-io] in the workspace Cargo.toml; delete this
# directory and the patch once upstream publishes for egui 0.36.

[package]
edition = "2021"
name = "egui_ltreeview"
version = "0.8.0"
authors = ["Leonard Schüngel <schuengel.leonard@gmail.com>"]
build = false
include = [
    "src/**/*.rs",
    "Cargo.toml",
    "LICENSE",
    "src/doc/doc.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A tree view widget for egui"
readme = "README.md"
keywords = [
    "egui",
    "gui",
    "widget",
    "tree",
]
categories = ["gui"]
license = "MIT"
repository = "https://github.com/LennysLounge/egui_ltreeview"

[package.metadata.docs.rs]
features = ["doc"]

[features]
default = []
doc = ["dep:make_table_of_contents"]
persistence = [
    "dep:serde",
    "egui/persistence",
    "uuid/serde",
]

[lib]
name = "egui_ltreeview"
path = "src/lib.rs"

[dependencies.egui]
version = "0.36"

[dependencies.make_table_of_contents]
version = "0.1.0"
optional = true

[dependencies.serde]
version = "1"
features = ["derive"]
optional = true

[dev-dependencies.eframe]
version = "0.36"

[dev-dependencies.egui_extras]
version = "0.36"
features = ["all_loaders"]

[dev-dependencies.uuid]
version = "1"
features = ["v4"]

[target.'cfg(target_arch = "wasm32")'.dev-dependencies.log]
version = "0.4.27"

[target.'cfg(target_arch = "wasm32")'.dev-dependencies.uuid]
version = "1"
features = [
    "v4",
    "js",
]

[target.'cfg(target_arch = "wasm32")'.dev-dependencies.wasm-bindgen-futures]
version = "0.4.50"

[target.'cfg(target_arch = "wasm32")'.dev-dependencies.web-sys]
version = "0.3.70"
